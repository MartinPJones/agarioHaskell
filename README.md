# Agar.io but in Haskell
Barely functions

# Install
Clone this repo, cd into the top-level directory of it, and run `cabal install`.

Once Cabal has finished installing, you can run `cabal build` and then `cabal run`